<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>To Do List</title>
  </head>
  <body>

    <% if(thisDay === "Saturday" || thisDay === "Sunday"){ %>
      <h1 style="color:red"><%= thisDay %> Todo List </h1>
    <%} else{ %>
      <h1 style="color:blue"><%= thisDay %> Todo List </h1>
    <% } %>

  </body>
</html>
